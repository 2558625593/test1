import React, { Component } from 'react';

class Logain extends Component {
    render() {
        return (
            <div>
                登录
            </div>
        );
    }
}

export default Logain;